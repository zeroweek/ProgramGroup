1. 关闭流程
	
	* listener 停止监听
	* 服务逻辑关闭：各模块close（包括所有连接的关闭）
	* 执行完所有未执行完的事件
	* 服务器uninit

	

2. UnInit

		如果某个模块的UnInit会调用其他模块的UnInit，则不能再析构函数中调用UnInit，因为其他模块可能
	在本模块之前先释放（析构），即如果此时在调用其他模块的UnInit则会出错。


3. 关闭socket流程

	？ 是否会出现：关闭事件未执行，而对方已经建立了新的连接的情况